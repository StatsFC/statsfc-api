<p>You authenticate to the Stats FC API via API key.</p>

<p>Include the API key from your <a href="https://statsfc.com/account" target="_blank">account page</a> as a <code>X-StatsFC-Key</code> header. For example:</p>

<div class="panel panel-default">
    <div class="panel-heading solo">
        <pre>curl -H "X-StatsFC-Key: {api_key}" https://dugout.statsfc.com/api/v2/…</pre>
    </div>
</div>

<p>To try the API before you sign up use the API key <code>apitest</code>. More in the <a href="#faqs">FAQs</a>.</p>
